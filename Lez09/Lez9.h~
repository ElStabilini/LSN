#include <iostream>
#include <fstream>
#include <ostream>
#include <cmath>
#include <iomanip>
#include <vector>
#include <string>
#include <algorithm>
#include "GenAlgs.h"

using namespace std;

//funzioni per l'esercizio
void InitializeRND(Random &rnd);

//dichiarazione variabili
Random rnd;
int numero_individui = 400;
int n_generazioni = 1000;
double selettivita = 1.2;
double mutabilita = 0.15;
int dimensioni = 32;
int n_riproduzioni = int(numero_individui/4);

ofstream out ("circonferenza.dat");
